# with-bin example

Library with CLI bin entry demonstrating NodeLibraryBuilder bin support.

- Source: `src/` (index.ts + cli.ts)
- Depends on: `@savvy-web/rslib-builder` via `workspace:*`
- Build: `turbo run build:dev build:prod`
