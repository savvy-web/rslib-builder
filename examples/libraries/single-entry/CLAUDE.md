# single-entry example

Toy calculator library demonstrating NodeLibraryBuilder with a single export.

- Source: `src/` (Calculator class, internal utils)
- Tests: `__test__/` (unit tests via vitest)
- Depends on: `@savvy-web/rslib-builder` via `workspace:*`
- Build: `turbo run build:dev build:prod`
