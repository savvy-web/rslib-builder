# multi-entry example

Library with multiple exports (./utils, ./types) demonstrating NodeLibraryBuilder multi-entry.

- Source: `src/`
- Depends on: `@savvy-web/rslib-builder` via `workspace:*`
- Build: `turbo run build:dev build:prod`
