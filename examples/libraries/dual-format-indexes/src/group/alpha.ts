/**
 * Nested export `./group/alpha`.
 *
 * @packageDocumentation
 */

/**
 * Adds two numbers.
 *
 * @param a - First addend
 * @param b - Second addend
 * @returns The sum of `a` and `b`
 *
 * @public
 */
export function add(a: number, b: number): number {
	return a + b;
}
