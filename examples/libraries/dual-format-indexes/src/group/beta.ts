/**
 * Nested export `./group/beta`.
 *
 * @packageDocumentation
 */

/**
 * Multiplies two numbers.
 *
 * @param a - First factor
 * @param b - Second factor
 * @returns The product of `a` and `b`
 *
 * @public
 */
export function multiply(a: number, b: number): number {
	return a * b;
}
